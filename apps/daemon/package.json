{
  "name": "@mcpot/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node --experimental-transform-types --disable-warning=ExperimentalWarning src/main.ts"
  },
  "dependencies": {
    "@mcpot/protocol": "workspace:*",
    "@mcpot/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
